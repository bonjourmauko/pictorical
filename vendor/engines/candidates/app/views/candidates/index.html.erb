<% content_for :body_content_left do %>
  <ul id="candidates">
    <% @candidates.each do |candidate| %>
      <li>
        <%= link_to candidate.name, candidate_url(candidate) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
